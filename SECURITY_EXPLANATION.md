# 🔐 อธิบายระบบ Security และการเก็บข้อมูล

## 📋 คำถาม: เก็บรหัสผ่านไว้ที่ไหน?

### คำตอบสั้นๆ:
- **รหัสผ่านของผู้ใช้ (User Password)**: เก็บใน **Backend Database** (แต่เป็น **hash** ไม่ใช่ plaintext)
- **รหัสผ่านของ Vault (Vault Password)**: **ไม่เก็บเลย** - ใช้เข้ารหัส master key แล้วเก็บ master key ที่เข้ารหัสแล้ว

## 🔍 อธิบายแบบละเอียด

### 1. รหัสผ่านของผู้ใช้ (User Password) - สำหรับ Login

#### เก็บที่ไหน?
```
Backend Database (SQLite/PostgreSQL)
└── users table
    └── password_hash (เก็บเป็น hash ไม่ใช่ plaintext)
```

#### กระบวนการ:

**ตอนสมัครสมาชิก:**
```
1. User กรอก: password = "mypassword123"
2. Mobile App ส่งไป Backend
3. Backend hash ด้วย bcrypt:
   password_hash = bcrypt.hash("mypassword123")
   → ได้: "$2b$10$abcdefghijklmnopqrstuvwxyz..."
4. เก็บ password_hash ใน database
5. ❌ ไม่เก็บ plaintext password เลย!
```

**ตอน Login:**
```
1. User กรอก: password = "mypassword123"
2. Mobile App ส่งไป Backend
3. Backend เอา password_hash จาก database
4. Backend เปรียบเทียบ:
   bcrypt.compare("mypassword123", password_hash)
5. ถ้าตรง → ส่ง JWT token กลับมา
6. ❌ Backend ไม่รู้ plaintext password
```

#### ตัวอย่างใน Database:
```sql
-- users table
id: "abc-123"
email: "user@example.com"
password_hash: "$2b$10$abcdefghijklmnopqrstuvwxyz..."  ← ไม่ใช่ plaintext!
username: "user1"
```

**สำคัญ**: Backend **ไม่รู้** plaintext password เลย! รู้แค่ hash เท่านั้น

---

### 2. รหัสผ่านของ Vault (Vault Password) - สำหรับเปิด Vault

#### เก็บที่ไหน?
```
❌ ไม่เก็บเลย! (Zero-knowledge architecture)
```

#### กระบวนการ:

**ตอนสร้าง Vault:**
```
1. User สร้าง Vault พร้อมรหัสผ่าน: "vaultpass123"
2. Mobile App สร้าง Master Key (256-bit random)
3. Mobile App เข้ารหัส Master Key ด้วย Vault Password:
   encrypted_master_key = AES.encrypt(master_key, vault_password)
4. เก็บ encrypted_master_key ใน:
   - Local Database (SQLite ในเครื่อง)
   - Backend Database (sync metadata)
5. ❌ ไม่เก็บ vault_password ที่ไหนเลย!
```

**ตอนเปิด Vault:**
```
1. User กรอก vault password: "vaultpass123"
2. Mobile App ดึง encrypted_master_key จาก database
3. Mobile App ถอดรหัส:
   master_key = AES.decrypt(encrypted_master_key, vault_password)
4. ถ้าถอดรหัสสำเร็จ → ใช้ master_key เข้ารหัส/ถอดรหัสไฟล์
5. ❌ vault_password ไม่ถูกส่งไป Backend เลย!
```

#### ตัวอย่างใน Database:
```sql
-- vaults table (Local + Backend)
id: "vault-456"
name: "My Photos"
encrypted_master_key: "base64_encoded_encrypted_data..."  ← Master Key ที่เข้ารหัสแล้ว
salt: "base64_encoded_salt..."
```

**สำคัญ**: 
- Vault Password **ไม่ถูกเก็บที่ไหนเลย**
- ถ้าลืม Vault Password → **ไม่สามารถกู้คืนได้** (Zero-knowledge)
- Backend **ไม่รู้** Vault Password

---

## 🏗️ ระบบหลังบ้าน (Backend) ทำอะไร?

### Backend เก็บอะไรบ้าง?

#### 1. User Information (ข้อมูลผู้ใช้)
```
✅ Email
✅ Username
✅ Password Hash (ไม่ใช่ plaintext)
✅ Created Date
❌ ไม่เก็บ: Plaintext Password
```

#### 2. Vault Metadata (ข้อมูล Vault)
```
✅ Vault Name
✅ Encrypted Master Key (เข้ารหัสแล้ว)
✅ Salt
✅ Created Date
❌ ไม่เก็บ: Vault Password
❌ ไม่เก็บ: Master Key (plaintext)
```

#### 3. File Metadata (ข้อมูลไฟล์)
```
✅ Encrypted File Name
✅ File Type
✅ File Size
✅ Created Date
❌ ไม่เก็บ: ไฟล์จริง (ไฟล์เก็บในเครื่อง)
❌ ไม่เก็บ: File Content
```

### Backend ไม่เก็บอะไร?

```
❌ Plaintext Passwords (ทั้ง User และ Vault)
❌ Master Keys (plaintext)
❌ ไฟล์จริง (Files)
❌ File Content
❌ Decrypted Data
```

---

## 📊 สรุปการเก็บข้อมูล

### User Password (สำหรับ Login)
```
┌─────────────────────────────────────┐
│  User กรอก: "mypassword123"         │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  Backend Hash: bcrypt               │
│  → "$2b$10$abc..."                   │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  Database: password_hash             │
│  (เก็บ hash ไม่ใช่ plaintext)       │
└─────────────────────────────────────┘
```

### Vault Password (สำหรับเปิด Vault)
```
┌─────────────────────────────────────┐
│  User กรอก: "vaultpass123"          │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  Mobile App:                         │
│  - สร้าง Master Key (random)        │
│  - เข้ารหัส Master Key               │
│  - เก็บ encrypted_master_key        │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  Database: encrypted_master_key     │
│  (Master Key ที่เข้ารหัสแล้ว)       │
└─────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  ❌ vault_password ไม่ถูกเก็บเลย!   │
└─────────────────────────────────────┘
```

---

## 🔒 Security Architecture

### Zero-Knowledge Architecture

```
┌─────────────────────────────────────────┐
│  Backend (Server)                        │
│  ┌───────────────────────────────────┐  │
│  │ รู้:                               │  │
│  │ ✅ User email                      │  │
│  │ ✅ Password hash                   │  │
│  │ ✅ Encrypted master keys           │  │
│  │ ❌ ไม่รู้:                          │  │
│  │   - Plaintext passwords            │  │
│  │   - Vault passwords                │  │
│  │   - Master keys (plaintext)        │  │
│  │   - File content                   │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  Mobile App (Device)                     │
│  ┌───────────────────────────────────┐  │
│  │ รู้:                               │  │
│  │ ✅ Vault password (ชั่วคราว)      │  │
│  │ ✅ Master key (ชั่วคราว)           │  │
│  │ ✅ Encrypted files                 │  │
│  │ ❌ ไม่เก็บ:                        │  │
│  │   - Vault password (ถาวร)         │  │
│  │   - Master key (ถาวร)             │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

---

## 💾 ข้อมูลเก็บที่ไหนบ้าง?

### 1. Backend Database (SQLite/PostgreSQL)

**เก็บ:**
- ✅ User accounts (email, password_hash)
- ✅ Vault metadata (name, encrypted_master_key, salt)
- ✅ File metadata (encrypted_name, size, type)

**ไม่เก็บ:**
- ❌ Plaintext passwords
- ❌ Files (ไฟล์จริง)
- ❌ Decrypted data

### 2. Local Device (Mobile App)

**เก็บ:**
- ✅ Encrypted files (ไฟล์ที่เข้ารหัสแล้ว)
- ✅ Local database (SQLite) - vault metadata
- ✅ JWT token (ใน secure storage)

**ไม่เก็บ:**
- ❌ Vault password (ถาวร)
- ❌ Master key (plaintext, ถาวร)
- ❌ Decrypted files (ชั่วคราวใน memory)

---

## 🔐 Security Features

### 1. Password Hashing
- **Algorithm**: bcrypt
- **Salt**: Random per user
- **Iterations**: 10 rounds
- **Result**: ไม่สามารถ reverse ได้

### 2. Encryption
- **Algorithm**: AES-256-GCM
- **Key**: 256-bit
- **IV**: Random per file
- **Result**: ไฟล์เข้ารหัสอย่างปลอดภัย

### 3. Key Management
- **Master Key**: Random 256-bit
- **Encryption**: AES-256
- **Storage**: Encrypted only
- **Result**: ไม่มีใครรู้ master key

---

## 🎯 สรุป

### User Password (Login)
- **เก็บที่**: Backend Database
- **รูปแบบ**: Hash (bcrypt)
- **Backend รู้ไหม**: ไม่รู้ plaintext

### Vault Password (เปิด Vault)
- **เก็บที่**: ไม่เก็บเลย!
- **รูปแบบ**: ใช้เข้ารหัส master key แล้วทิ้ง
- **Backend รู้ไหม**: ไม่รู้เลย (Zero-knowledge)

### Files (ไฟล์)
- **เก็บที่**: Local Device (เครื่องผู้ใช้)
- **รูปแบบ**: Encrypted (AES-256-GCM)
- **Backend รู้ไหม**: ไม่รู้ (เก็บแค่ metadata)

---

## ⚠️ ข้อควรระวัง

1. **ถ้าลืม User Password**:
   - ❌ ไม่สามารถ reset ได้ (ถ้าไม่มี email recovery)
   - ✅ แต่สามารถสร้าง account ใหม่ได้

2. **ถ้าลืม Vault Password**:
   - ❌ **ไม่สามารถกู้คืนได้เลย!**
   - ❌ ไฟล์ทั้งหมดจะสูญหาย
   - ⚠️ **สำคัญมาก**: จำรหัสผ่านให้ดี!

3. **Backend ไม่สามารถอ่านไฟล์ได้**:
   - ✅ ไฟล์เก็บในเครื่อง (Local)
   - ✅ เข้ารหัสด้วย master key
   - ✅ Backend ไม่มี master key

---

## 🔍 ตัวอย่างการทำงาน

### Scenario: User สร้าง Vault และอัปโหลดไฟล์

```
1. User สมัครสมาชิก
   → Backend: เก็บ password_hash
   → Backend: ไม่รู้ plaintext password

2. User Login
   → Backend: เปรียบเทียบ password hash
   → Backend: ส่ง JWT token
   → Backend: ไม่รู้ plaintext password

3. User สร้าง Vault
   → Mobile: สร้าง master key (random)
   → Mobile: เข้ารหัส master key ด้วย vault password
   → Mobile: เก็บ encrypted_master_key
   → Backend: เก็บ encrypted_master_key (metadata)
   → Backend: ไม่รู้ vault password
   → Backend: ไม่รู้ master key (plaintext)

4. User อัปโหลดไฟล์
   → Mobile: เข้ารหัสไฟล์ด้วย master key
   → Mobile: เก็บไฟล์เข้ารหัสในเครื่อง
   → Backend: เก็บแค่ metadata (ชื่อ, ขนาด, ประเภท)
   → Backend: ไม่รู้ไฟล์จริง
   → Backend: ไม่สามารถอ่านไฟล์ได้
```

---

## ✅ Security Best Practices ที่ใช้

1. ✅ **Password Hashing**: bcrypt (ไม่เก็บ plaintext)
2. ✅ **Encryption**: AES-256-GCM (มาตรฐานสูง)
3. ✅ **Zero-Knowledge**: Backend ไม่รู้ passwords
4. ✅ **Local Storage**: ไฟล์เก็บในเครื่อง
5. ✅ **Secure Storage**: JWT token ใน secure storage
6. ✅ **Key Derivation**: PBKDF2 (200,000 iterations)

---

## 🎓 สรุปสั้นๆ

**User Password (Login)**:
- เก็บใน Backend (เป็น hash)
- Backend ไม่รู้ plaintext

**Vault Password (เปิด Vault)**:
- ไม่เก็บเลย!
- ใช้เข้ารหัส master key แล้วทิ้ง
- Backend ไม่รู้เลย

**Files (ไฟล์)**:
- เก็บในเครื่อง (Local)
- เข้ารหัสแล้ว
- Backend ไม่รู้เนื้อหา

**Backend ทำอะไร**:
- จัดการ User accounts
- Sync vault metadata
- ไม่รู้ passwords
- ไม่รู้ไฟล์จริง
